import 'package:flutter/material.dart';

class BullDog extends StatelessWidget {
  const BullDog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('BullDog'),
        ),
      ),
    );
  }
}
