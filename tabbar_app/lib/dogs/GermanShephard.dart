import 'package:flutter/material.dart';

class GermanShephard extends StatelessWidget {
  const GermanShephard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('GermanShephard'),
        ),
      ),
    );
  }
}
