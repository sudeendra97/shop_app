import 'package:flutter/material.dart';

class GoldenRetriever extends StatelessWidget {
  const GoldenRetriever({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('GoldenRetriever'),
        ),
      ),
    );
  }
}
