import 'package:flutter/material.dart';

class nestedcontainer2 extends StatelessWidget {
  const nestedcontainer2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(50),
      color: Colors.green,
    );
  }
}
